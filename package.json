{
  "name": "fakeboot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "wrangler": "^4.14.0"
  },
  "dependencies": {
    "vis-data": "^7.1.9",
    "vis-network": "^9.1.9"
  }
}
